name: Build Serag OS Android Widget

on:
  push:
    branches: [main]
    paths:
      - 'android-widget/**'
      - '.github/workflows/build-android-widget.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android 35 SDK
        run: sdkmanager 'platforms;android-35' 'build-tools;35.0.0'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        run: gradle -p android-widget :app:assembleDebug --stacktrace

      - name: Prepare APK
        run: cp android-widget/app/build/outputs/apk/debug/app-debug.apk Serag-OS-Android-v0.1.0.apk

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: Serag-OS-Android-v0.1.0
          path: Serag-OS-Android-v0.1.0.apk

      - name: Publish APK release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view serag-android-v0.1.0 >/dev/null 2>&1; then
            gh release upload serag-android-v0.1.0 Serag-OS-Android-v0.1.0.apk --clobber
          else
            gh release create serag-android-v0.1.0 Serag-OS-Android-v0.1.0.apk --title 'Serag OS Android v0.1.0' --notes 'First experimental Android build with a Samsung/Android home-screen widget for water, English words, and Serag voice assistant.' --prerelease
          fi
